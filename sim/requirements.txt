# Baseline deps for `python3 -m unittest discover -s sim/tests -p "test_*.py"`
# without import-time failures (telemetry + NumPy-backed tests).
# Full physics / QuTiP / plotting stack: pip install -r sim/requirements-optional.txt
numpy>=1.19.0
pydantic>=2.0.0
